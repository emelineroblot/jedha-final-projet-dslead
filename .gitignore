# Environnements Python
venv/
.venv/
.venv313/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/

# Data (géré par DVC — les .dvc pointer files sont committés, pas les données)
data/*.csv
data/**/*.csv
mlruns/
reports/*.html
reports/*.json
reports/history/

# Artefacts modèle exportés (poussés sur HF Space, pas dans le repo principal)
model_artifacts/

# Repos HF Spaces (git imbriqués, gérés séparément)
hf-space/
hf-mlflow/
hf-airflow/
hf-demo/

# Exports SeoLap (données utilisateurs — non versionnées)
exports/


# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Claude & contexte (documents de cadrage, non versionnés)
contexte/
.claude/
docs/soutenance*
docs/audit.md
docs/journal-session.md
docs/livrables-soutenances.md
docs/attendus-theoriques.md
.mcp.json

# Secrets
.env
.env.*
!.env.example
secrets/

# Logs
*.log
logs/

# Jupyter checkpoints
.ipynb_checkpoints/

# DVC cache
.dvc/cache/
.dvc/tmp/
/model_artifacts

# Overrides compose locaux (build args machine-spécifiques, ex. Avast)
docker/**/docker-compose.override.yml

# Overrides compose locaux (build args machine-spécifiques, ex. Avast) — garder le .example
docker/**/docker-compose.override.yml

# Wheels pré-téléchargés pour les builds Docker hors-ligne (voir P33) — dossier vide en CI
wheels/*
!wheels/.gitkeep
