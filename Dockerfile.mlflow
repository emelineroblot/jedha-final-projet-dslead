# Serveur MLflow (Tracking + Model Registry). Même version majeure que le client (requirements-api.txt).
# psycopg2-binary : driver PostgreSQL absent de l'image officielle ghcr.io/mlflow/mlflow.
FROM python:3.11-slim
RUN pip install --no-cache-dir mlflow==3.12.0 psycopg2-binary==2.9.13
EXPOSE 5000
