import pandas as pd


def engineer_features(df: pd.DataFrame) -> pd.DataFrame:
    # TODO: implémenter lors de la phase 2
    # - Features temporelles (tenure_days, days_since_last_renewal)
    # - Features d'usage (error_rate, beta_feature_ratio, usage_trend)
    # - Features financières (mrr_delta, has_upgraded, has_downgraded)
    # - Features de support (last_ticket_days_ago)
    # - Encodage catégoriel (plan_tier, industry, referral_source)
    return df
